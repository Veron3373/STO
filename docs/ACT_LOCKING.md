# Механізм блокування актів

## Опис
Реалізовано механізм блокування актів для запобігання одночасного редагування одного акту кількома користувачами.

## Як це працює

### При відкритті акту:
1. Система перевіряє стовпець `act_on_off` в таблиці `acts`
2. Якщо стовпець пустий або містить ПІБ поточного користувача - акт відкривається нормально
3. Якщо стовпець містить ПІБ іншого користувача:
   - Відображається повідомлення: "⚠️ Акт відкритий користувачем: [ПІБ]"
   - Header модального вікна змінює колір на червоний (#8B0000)
   - Кнопка "💾 Зберегти зміни" блокується
   - Кнопки в header також змінюють колір на червоний

### При закритті акту:
1. Система очищає стовпець `act_on_off` (встановлює NULL)
2. Акт стає доступним для редагування іншими користувачами

## Міграція бази даних

Для роботи функціоналу потрібно виконати SQL міграцію:

```bash
# Виконайте файл migrations/add_act_on_off_column.sql в Supabase SQL Editor
```

Або виконайте SQL вручну:
```sql
ALTER TABLE acts ADD COLUMN IF NOT EXISTS act_on_off TEXT;
COMMENT ON COLUMN acts.act_on_off IS 'ПІБ користувача, який відкрив акт для редагування';
```

## Файли, що були змінені

1. **src/ts/roboha/zakaz_naraudy/actLock.ts** (новий файл)
   - `lockAct()` - блокує акт при відкритті
   - `unlockAct()` - розблоковує акт при закритті
   - `setLockedUI()` - встановлює UI в режим блокування
   - `setUnlockedUI()` - встановлює UI в режим розблокування

2. **src/ts/roboha/zakaz_naraudy/modalMain.ts**
   - Додано виклик `lockAct()` при відкритті акту
   - Додано виклик `setLockedUI()` якщо акт заблоковано

3. **src/ts/roboha/zakaz_naraudy/modalUI.ts**
   - Додано виклик `unlockAct()` при закритті модального вікна

## Тестування

1. Відкрийте акт від імені користувача №1
2. Спробуйте відкрити той самий акт від імені користувача №2
3. Перевірте що:
   - Користувач №2 бачить повідомлення про блокування
   - Header червоний
   - Кнопка збереження заблокована
4. Закрийте акт від імені користувача №1
5. Перевірте що користувач №2 може тепер редагувати акт

## Примітки

- Блокування працює на рівні бази даних
- Якщо користувач закриє браузер без закриття акту, акт залишиться заблокованим
- Адміністратор може вручну очистити `act_on_off` через Supabase Dashboard
