# 💛 Реалізація функції slusarsOn - Інструкція з впровадження

## ✅ Що вже зроблено

### 1. Створено нові файли:

- ✅ [supabase/add_slusarsOn_column.sql](supabase/add_slusarsOn_column.sql) - SQL скрипт для додавання колонки
- ✅ [src/ts/roboha/zakaz_naraudy/inhi/vikno_slusar_confirm.ts](src/ts/roboha/zakaz_naraudy/inhi/vikno_slusar_confirm.ts) - Модальне вікно підтвердження
- ✅ [src/scss/robocha/zakaz_narayd/\_vikno_slusar_confirm.scss](src/scss/robocha/zakaz_narayd/_vikno_slusar_confirm.scss) - Стилі для модального вікна

### 2. Створено допоміжні файли з готовим кодом:

- ✅ [src/ts/roboha/tablucya/slusarsOn_tablucya_functions.ts](src/ts/roboha/tablucya/slusarsOn_tablucya_functions.ts) - Функції для tablucya.ts
- ✅ [src/ts/roboha/zakaz_naraudy/slusarsOn_modal_functions.ts](src/ts/roboha/zakaz_naraudy/slusarsOn_modal_functions.ts) - Функції для modalMain.ts
- ✅ [src/ts/roboha/zakaz_naraudy/inhi/slusarsOn_knopka_zamok_logic.ts](src/ts/roboha/zakaz_naraudy/inhi/slusarsOn_knopka_zamok_logic.ts) - Логіка для knopka_zamok.ts

⚠️ **ВАЖЛИВО:** Ці файли - це **ШАБЛОНИ/ПРИКЛАДИ КОДУ**, а не робочі модулі! Помилки TypeScript в них - це нормально, оскільки імпорти вказані для інших файлів. Скопіюйте код з цих файлів та вставте його в відповідні робочі файли згідно інструкцій.

### 3. Оновлено існуючі файли:

- ✅ [src/ts/roboha/tablucya/users.ts](src/ts/roboha/tablucya/users.ts) - Додано функцію `canSlusarCompleteTasks()`
- ✅ [src/scss/robocha/zakaz_narayd/\_zakaz_narayd.scss](src/scss/robocha/zakaz_narayd/_zakaz_narayd.scss) - Додано клас `.zakaz_narayd-header-slusar-on`
- ✅ [src/scss/robocha/tablucya/\_tablucay.scss](src/scss/robocha/tablucya/_tablucay.scss) - Додано клас `.row-slusar-on`
- ✅ [src/scss/main.scss](src/scss/main.scss) - Додано імпорт `vikno_slusar_confirm`

---

## 🚨 Важлива зміна вимог!

**НОВА ВИМОГА:** Золотистий колір відображається **ТІЛЬКИ**:

1. ✅ **Для Адміністратора** - бачить ВСІ золотисті акти
2. ✅ **Для конкретного Приймальника** - бачить ТІЛЬКИ ті акти, де `act.pruimalnyk === currentUserName`

**Інші Приймальники НЕ бачать золотистий колір на чужих актах!**

---

## 📋 Що потрібно зробити вручну

### Крок 1: Виконати SQL скрипт

```bash
# Відкрити Supabase Dashboard → SQL Editor
# Виконати вміст файлу supabase/add_slusarsOn_column.sql
```

### Крок 2: Оновити файл `src/ts/roboha/tablucya/tablucya.ts`

#### 2.1. Додати імпорти на початку файлу:

```typescript
import { userName as currentUserName } from "./users";
```

#### 2.2. Знайти функцію `renderActsRows` та оновити код після `row.classList.add(isClosed ? "row-closed" : "row-open");`:

```typescript
row.classList.add(isClosed ? "row-closed" : "row-open");

// 💛 ПЕРЕВІРКА slusarsOn ДЛЯ ЗОЛОТИСТОГО ФАРБУВАННЯ (ТІЛЬКИ ДЛЯ ВІДКРИТИХ АКТІВ)
// ✨ НОВИНКА: Для Приймальника показувати тільки якщо pruimalnyk === currentUserName
const shouldShowSlusarsOn =
  act.slusarsOn === true &&
  !isClosed &&
  (userAccessLevel === "Адміністратор" ||
    (userAccessLevel === "Приймальник" && act.pruimalnyk === currentUserName));

if (shouldShowSlusarsOn) {
  row.classList.add("row-slusar-on");
}

// ПЕРЕВІРКА ПІДСВІТКИ (СИНЯ РУЧКА)
if (act.act_id && modifiedActIds.has(Number(act.act_id))) {
  row.classList.add("act-modified-blue-pen");
}
```

#### 2.3. Додати функції з файлу `slusarsOn_tablucya_functions.ts`:

Скопіювати функції `subscribeToSlusarsOnChanges()` та `updateRowSlusarsOnInDom()` в файл `tablucya.ts`.

#### 2.4. Додати виклик підписки в кінці функції ініціалізації таблиці:

```typescript
// 💛 АКТИВУЄМО REALTIME ПІДПИСКУ НА ЗМІНИ slusarsOn
subscribeToSlusarsOnChanges();
```

---

### Крок 3: Оновити файл `src/ts/roboha/zakaz_naraudy/modalMain.ts`

#### 3.1. Додати імпорти на початку файлу:

```typescript
import { userName as currentUserName } from "../tablucya/users";
```

#### 3.2. Знайти місце де рендериться HTML акту та оновити:

```typescript
const showLockButton = canShowLockButton;

// 💛 ПЕРЕВІРКА slusarsOn ДЛЯ ФАРБУВАННЯ ЗАГОЛОВКА (ТІЛЬКИ ДЛЯ ВІДКРИТИХ АКТІВ)
// ✨ НОВИНКА: Для Приймальника показувати тільки якщо pruimalnyk === currentUserName
const isClosed = !!act.date_off;
const shouldShowSlusarsOn = act.slusarsOn === true && !isClosed &&
  (userAccessLevel === "Адміністратор" ||
   (userAccessLevel === "Приймальник" && act.pruimalnyk === currentUserName));

const headerClass = shouldShowSlusarsOn
  ? 'zakaz_narayd-header zakaz_narayd-header-slusar-on'
  : 'zakaz_narayd-header';

body.innerHTML = `
  <div class="${headerClass}">
    <div class="zakaz_narayd-header-info">
      <h1>Shlif service</h1>
      ...
```

#### 3.3. Додати функцію з файлу `slusarsOn_modal_functions.ts`:

Скопіювати функцію `subscribeToSlusarsOnForModal()` в файл `modalMain.ts`.

#### 3.4. Після відкриття акту додати виклик підписки:

```typescript
// 💛 Підписка на зміни slusarsOn (тільки для Приймальника його актів та Адміністратора)
subscribeToSlusarsOnForModal(actId, act.pruimalnyk);
```

---

### Крок 4: Оновити файл `src/ts/roboha/zakaz_naraudy/inhi/knopka_zamok.ts`

#### 4.1. Додати імпорти на початку файлу:

```typescript
import { canSlusarCompleteTasks } from "../../tablucya/users";
import { showSlusarConfirm } from "./vikno_slusar_confirm";
```

#### 4.2. Додати логіку для Слюсаря НА ПОЧАТКУ обробника кліку кнопки замка:

Скопіювати код з файлу `slusarsOn_knopka_zamok_logic.ts` - весь блок з коментарем "🔵 СПЕЦІАЛЬНА ЛОГІКА ДЛЯ СЛЮСАРЯ".

#### 4.3. В коді закриття акту оновити:

Знайти місце де встановлюється `date_off` та додати `slusarsOn: false`:

```typescript
const { error: updateError } = await supabase
  .from("acts")
  .update({
    date_off: new Date().toISOString(),
    slusarsOn: false, // ← АВТОМАТИЧНЕ СКИДАННЯ
  })
  .eq("act_id", actId);
```

---

### Крок 5: Оновити файл `src/ts/roboha/zakaz_naraudy/modalUI.ts`

Додати відписку від каналу при закритті модального вікна:

```typescript
closeBtn?.addEventListener("click", () => {
  newModalOverlay.classList.add("hidden");
  globalCache.currentActId = null;

  // 💛 Відписуємося від realtime каналу slusarsOn
  if ((window as any).slusarsOnChannel) {
    const { supabase } = require("../../vxid/supabaseClient");
    supabase.removeChannel((window as any).slusarsOnChannel);
    (window as any).slusarsOnChannel = null;
  }
});
```

---

### Крок 6: Налаштувати права в базі даних

```sql
-- Перевірити налаштування в таблиці settings
SELECT * FROM settings WHERE setting_id = 3;

-- Якщо потрібно, оновити право для Слюсаря
UPDATE settings
SET "Слюсар" = true
WHERE setting_id = 3;
```

---

### Крок 7: Зібрати проект

```bash
npm run deploy2
```

---

## 🧪 Перевірка роботи

### Для Слюсаря:

1. Увійти як Слюсар
2. Відкрити будь-який акт
3. Натиснути на 🗝️
4. Побачити красиве модальне вікно з кнопками "Підтвердити" та "Відмінити"
5. Підтвердити виконання робіт
6. Заголовок має стати золотистим
7. Перевірити, що акт НЕ закрився (date_off = null)

### Для Приймальника (тільки свої акти):

1. Увійти як Приймальник
2. Відкрити таблицю актів
3. **Побачити золотисті рядки ТІЛЬКИ для тих актів, де `pruimalnyk === currentUserName`**
4. **НЕ бачити золотисті рядки для актів інших Приймальників**
5. Відкрити свій акт (де `pruimalnyk === currentUserName`) з `slusarsOn = true` → заголовок золотистий
6. Відкрити чужий акт (де `pruimalnyk !== currentUserName`) з `slusarsOn = true` → заголовок НЕ золотистий

### Для Адміністратора:

1. Увійти як Адміністратор
2. Відкрити таблицю актів
3. **Побачити золотисті рядки для ВСІХ актів з `slusarsOn = true`**
4. Відкрити будь-який акт з `slusarsOn = true` → заголовок золотистий

### Realtime перевірка:

1. Слюсар натискає на 🗝️ в акті
2. Приймальник (якщо це його акт) миттєво бачить золотистий рядок в таблиці
3. Адміністратор миттєво бачить золотистий рядок в таблиці
4. Інші Приймальники НЕ бачать золотистий рядок

### Автоматичне скидання:

1. Закрити акт будь-якою роллю → `slusarsOn` автоматично стає `false`
2. Перевірити, що закритий акт НЕ має золотистого кольору навіть якщо в БД `slusarsOn = true`

---

## 📊 Структура бази даних

### Таблиця `acts`

**Нова колонка:**

- `slusarsOn` — `BOOLEAN`, за замовчуванням `false`
- `pruimalnyk` — `TEXT`, ім'я Приймальника для цього акту (вже існує)

### Таблиця `settings`

**Використовується запис:**

- `setting_id = 3`
- Колонка: `Слюсар`
- Значення: `true/false`

---

## 🎨 Колірна схема

### Золотистий градієнт:

**Заголовок акту:**

- Світлий: `#ffc107` (яскраво-золотистий)
- Темний: `#ffa000` (темно-золотистий)

**Рядки таблиці:**

- Світлий: `#fff8e1` (дуже світло-золотистий / кремовий)
- Середній: `#ffe082` (світло-золотистий)
- Темний (hover): `#ffd54f` (середньо-золотистий)
- Текст: `#ff6f00` (помаранчевий), жирність `600`

---

## 🔑 Ключові особливості

1. ✅ Слюсар може позначати завершення робіт без закриття акту
2. ✅ **Приймальник бачить золотистий колір ТІЛЬКИ для своїх актів (де `pruimalnyk === currentUserName`)**
3. ✅ **Адміністратор бачить золотистий колір для ВСІХ актів**
4. ✅ Автоматичне скидання `slusarsOn = false` при закритті акту
5. ✅ Realtime оновлення з фільтрацією по `pruimalnyk` для Приймальника
6. ✅ Золотистий колір відображається ТІЛЬКИ для відкритих актів
7. ✅ Красиві модальні вікна підтвердження з зеленою/червоною кнопками

---

## 📞 Підтримка

Якщо виникнуть питання або проблеми, зверніться до автора: GitHub Copilot (Claude Sonnet 4.5) + Alim

**Дата створення:** 1 січня 2026 року
