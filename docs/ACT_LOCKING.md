# Механізм блокування актів

## Опис
Реалізовано механізм блокування актів для запобігання одночасного редагування одного акту кількома користувачами. Включає автоматичне очищення блокування при закритті/перезавантаженні сторінки.

## Як це працює

### При відкритті акту:
1. Система перевіряє стовпець `act_on_off` в таблиці `acts`
2. Якщо стовпець пустий або містить ПІБ поточного користувача - акт відкривається нормально
3. Якщо стовпець містить ПІБ іншого користувача:
   - Відображається повідомлення: "⚠️ Акт відкритий користувачем: [ПІБ]"
   - Header модального вікна змінює колір на червоний (#8B0000)
   - Кнопка "💾 Зберегти зміни" блокується
   - Кнопки в header також змінюють колір на червоний
4. **Встановлюється Realtime підписка** на зміни `act_on_off` для цього акту
5. **Встановлюється `beforeunload` обробник** для автоматичного очищення при закритті сторінки

### При закритті акту користувачем №1:
1. Система очищає стовпець `act_on_off` (встановлює NULL)
2. **Користувач №2 автоматично отримує сповіщення** через Realtime підписку:
   - Відображається повідомлення: "✅ Акт розблоковано. Редагування дозволено."
   - Header повертається до зеленого кольору (#1c4a28)
   - Кнопка "💾 Зберегти зміни" розблоковується
   - Кнопки в header повертаються до оригінального кольору
   - **Автоматично записується ПІБ користувача №2** в `act_on_off`

### При закритті/перезавантаженні сторінки:
1. **Спрацьовує `beforeunload` обробник**
2. Система автоматично очищає `act_on_off` через `fetch` з `keepalive: true`
3. Це працює навіть якщо користувач:
   - Закриває вкладку браузера
   - Перезавантажує сторінку (F5)
   - Закриває весь браузер
   - Переходить на іншу сторінку

### При закритті модального вікна:
1. Система очищає стовпець `act_on_off`
2. Очищається Realtime підписка на `act_on_off`
3. Очищається `beforeunload` обробник
4. Акт стає доступним для редагування іншими користувачами

## Міграція бази даних

Для роботи функціоналу потрібно виконати SQL міграцію:

```bash
# Виконайте файл migrations/add_act_on_off_column.sql в Supabase SQL Editor
```

Або виконайте SQL вручну:
```sql
ALTER TABLE acts ADD COLUMN IF NOT EXISTS act_on_off TEXT;
COMMENT ON COLUMN acts.act_on_off IS 'ПІБ користувача, який відкрив акт для редагування';
```

## Файли, що були змінені

1. **src/ts/roboha/zakaz_naraudy/actLock.ts** (новий файл)
   - `lockAct()` - блокує акт при відкритті
   - `unlockAct()` - розблоковує акт при закритті
   - `setLockedUI()` - встановлює UI в режим блокування
   - `setUnlockedUI()` - встановлює UI в режим розблокування
   - `setupActLockRealtimeSubscription()` - встановлює Realtime підписку на зміни act_on_off
   - `cleanupActLockSubscription()` - очищає Realtime підписку

2. **src/ts/roboha/zakaz_naraudy/modalMain.ts**
   - Додано виклик `lockAct()` при відкритті акту
   - Додано виклик `setLockedUI()` якщо акт заблоковано іншим користувачем
   - Додано виклик `setupActLockRealtimeSubscription()` для відстеження змін

3. **src/ts/roboha/zakaz_naraudy/modalUI.ts**
   - Додано виклик `unlockAct()` при закритті модального вікна
   - Додано виклик `cleanupActLockSubscription()` для очищення підписки

## Тестування

### Сценарій 1: Базове блокування
1. Відкрийте акт від імені користувача №1
2. Спробуйте відкрити той самий акт від імені користувача №2
3. Перевірте що:
   - Користувач №2 бачить повідомлення про блокування
   - Header червоний (#8B0000)
   - Кнопка збереження заблокована

### Сценарій 2: Автоматичне розблокування (Realtime)
1. Користувач №1 відкриває акт
2. Користувач №2 відкриває той самий акт (бачить червоний UI)
3. **Користувач №1 закриває акт** (натискає ×)
4. Перевірте що **користувач №2 автоматично** отримує:
   - Повідомлення: "✅ Акт розблоковано. Редагування дозволено."
   - Header змінюється на зелений (#1c4a28)
   - Кнопка збереження розблоковується
   - **БЕЗ перезавантаження сторінки!**

### Сценарій 3: Повторне відкриття
1. Закрийте акт від імені користувача №1
2. Перевірте що користувач №2 може тепер редагувати акт
3. Користувач №2 відкриває акт знову
4. Перевірте що акт відкривається нормально (зелений UI)

## Примітки

- Блокування працює на рівні бази даних
- **Realtime підписка** автоматично розблоковує акт для інших користувачів
- Якщо користувач закриє браузер без закриття акту, акт залишиться заблокованим
- Адміністратор може вручну очистити `act_on_off` через Supabase Dashboard
- Підписка автоматично очищується при закритті модального вікна

## Технічні деталі

### Realtime підписка
- Використовується Supabase Realtime для відстеження змін `act_on_off`
- Підписка встановлюється для кожного відкритого акту окремо
- Канал підписки: `act_lock_{actId}`
- Відстежуються події: `UPDATE` на таблиці `acts`
- Автоматичне очищення при закритті модального вікна запобігає витоку пам'яті

### Безпека
- Перевірка блокування відбувається на рівні бази даних
- Користувач може розблокувати тільки свій власний акт
- У випадку помилки система дозволяє відкрити акт (fail-safe)
